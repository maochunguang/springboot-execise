server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/execise
    username: root
    password: MyNewPass

mybatis:
  mapperLocations: classpath:mappers/*.xml
  type-aliases-package: com.mcg.execise.entity